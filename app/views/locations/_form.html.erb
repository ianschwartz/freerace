<%= form_for [@location.race, @location] do |f| %>
	<%= f.text_field :name, placeholder: "Name of Location...", :required => true %>
	<%= f.text_field :lat, placeholder: "Latitude", :required => true %>
	<%= f.text_field :lng, placeholder: "Longitude", :required => true %>
	<%= f.submit "Submit" %>
<% end %>